/// @file library.h Provides simple function to test the library setup

/// Just prints "Hello World!".
/**
 * Literally just prints "Hello World!".
 */
void HelloWorld();
/// Adds 1 to the passed integer
/**
 * @param[in] a - the value it should add one to
 * @return returns a + 1
 */
int AddOne(int a);